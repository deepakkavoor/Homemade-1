<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student.homemade">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="ANDROID.PERMISSION.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_home_made_round2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_home_made_round2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForgotActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar" />
        <activity
            android:name=".SliderActivity"
            android:label="SliderIntro" />
        <activity
            android:name=".SplashActivity"
            android:label="SplashScreen" />
        <activity android:name=".ui.ConsumerDetailsFragment" />
        <activity android:name=".SignUpSeller" />
        <activity
            android:name=".SignUpBuyer"
            android:label="@string/title_activity_sign_up_buyer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StartPage" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="Homemade" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RatingandReviewActivity"
            android:label="Ratings and Reviews"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CancelOrderFragment"
            android:label="Cancel Order"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ChooseActivity"
            android:label="Lunch"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".OrdersHistoryActivity"
            android:label="Lunch"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CurrentOrdersActivity"
            android:label="Lunch"
            android:theme="@style/AppTheme" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ReviewDisplayActivity" />
        <activity android:name=".UploadActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".MassDiscountActivity" />
        <activity android:name=".SubscriptionDiscountActivity"></activity>
    </application>

</manifest>